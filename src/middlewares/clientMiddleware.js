/**
 * Middleware for geting or setting client model
 *
 * @param  {} request
 * @param  {} response
 * @param  {} next
 * @return  void
 */
const clientMiddleware = ({}) => {

    return (request, response, next) => {

        next()
    }
}

module.exports = clientMiddleware